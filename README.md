A cross-compiling toolchain for C projects including:
- Zig
- CMake
- cmocka

Run `./configure` to download the Zig compiler and build remaining dependencies from source.

For example usage, see https://github.com/mrfitzy/soup.
